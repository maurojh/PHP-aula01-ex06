"# PHP-aula01-ex06" 

A função str_replace() procura uma palavra e troca por outra dentro de um texto:

str_replace("palavra", "outra", "texto");

Sabendo que o elemento <mark> marca um texto em amarelo, implemente um website em PHP que recebe um texto e uma palavra e mostra o texto com a palavra marcada quando houver.